package com.pixelservices.server;

import com.pixelservices.components.FlashConfiguration;
import com.pixelservices.components.FlashServer;

public class CobraServer extends FlashServer {
    public CobraServer(int port, FlashConfiguration config) {
        super(port, config);
    }

    private void registerRoutes()
}
